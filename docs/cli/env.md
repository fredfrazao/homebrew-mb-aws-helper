# env

Environment selection and export helpers.

## Command

```bash
mb-aws-helper env --help
```

## Help output

```text
🩺 mb doctor (v1.9.0-dev)

usage: mb-aws-helper env [-h] [--service {artifactory,gitlab}] env

positional arguments:
  env                   prod | sandbox | int

options:
  -h, --help            show this help message and exit
  --service {artifactory,gitlab}
                        Service to generate AWS_PROFILE for (default:
                        artifactory)
```

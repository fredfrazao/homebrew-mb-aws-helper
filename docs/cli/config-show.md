# config show

Show the effective merged configuration.

## Command

```bash
mb-aws-helper config show --help
```

## Help output

```text
usage: mb-aws-helper config show [-h] [--json]

options:
  -h, --help  show this help message and exit
  --json      Output JSON
```

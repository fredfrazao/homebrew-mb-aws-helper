# config init

Create the local config file if it does not exist.

## Command

```bash
mb-aws-helper config init --help
```

## Help output

```text
usage: mb-aws-helper config init [-h] [--force]

options:
  -h, --help  show this help message and exit
  --force     Overwrite existing config file
```
